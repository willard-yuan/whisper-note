/// Minimal SwiftUI building blocks for streaming speech apps.
///
/// - ``TranscriptionView`` — scrolling transcript with finals + partial line
/// - ``TranscriptionStore`` — `@Observable` adapter for any streaming ASR
public enum SpeechUI {}
